{
    // create a file 'appsettings.test.json' with the same structure
    // set 'Copy to output directory' to 'Copy if newer'
    // then, fill the values in there
    // this file is for reference only
    "ApiKey": "<api-key>"
}